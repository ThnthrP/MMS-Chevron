node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Prisma generated client (gen ใหม่ตอน build ทุกครั้ง ไม่ควร commit)
node_modules/.prisma

# logs / OS / editor
*.log
.DS_Store

# uploads (ถ้ามี runtime uploads)
uploads/